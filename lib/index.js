import toBuilder from 'lib/transform'
import format from 'lib/utils/formatter'
import print from 'lib/utils/print'
import h, { builtinComponents as components } from 'lib/h'
import * as shorthand from 'lib/components/shorthand'

// named export
export {
  toBuilder,
  format,
  print,
  components,
  shorthand,
  h
}

// default export
export default {
  toBuilder,
  format,
  print,
  components,
  shorthand,
  h
}
